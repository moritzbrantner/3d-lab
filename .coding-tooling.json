{
  "schemaVersion": 1,
  "profile": "repository-foundation-v1",
  "tiers": {
    "performance": ["benchmark:smoke"]
  },
  "requiredCapabilities": ["benchmark:smoke"],
  "capabilityCommands": {
    ".": {
      "benchmark:smoke": ["bun", "scripts/performance-smoke.mjs"]
    }
  },
  "merge": {
    "authority": "hosted",
    "requiredChecks": ["validate"]
  }
}
