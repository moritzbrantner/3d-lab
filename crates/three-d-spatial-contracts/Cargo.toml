[package]
name = "three-d-spatial-contracts"
version = "0.1.0"
edition = "2024"
description = "Versioned renderer-independent spatial annotation and camera interchange contracts"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
